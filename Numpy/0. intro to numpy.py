import numpy as np

# my_list = [1, 2, 3, 4,]
# my_list = my_list * 2 #makes it appeare twice, next to each other
# print(my_list)

# a numpy array is superior to a python list

array = np.array([1, 2, 3, 4])
array = array * 2 #this time we multiplied the numbers by 2
print(array)

#if you want the type of array. you can typecast it. like so:
print(type(array)) #results :<class 'numpy.ndarray'> (nd stands for "n-dimensional") array